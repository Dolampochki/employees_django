{# Load the tag library #}
{% load bootstrap5 %}

{# Load CSS and JavaScript #}
{% bootstrap_css %}
{% bootstrap_javascript %}

<body>
    <div class="container">
        <header>
            {% include "employees/menu.html" %}
        </header>
        <div class="row mt-4">
            <div class="col">
                {% for item in menu_list %}
                    {% if template_name == item.url %}
                        <h2 class="mb-4">{{ item.name }}</h2>
                        {%include template_name %}
                        {% endif %}
                {% endfor %}
                {% if error %}
                    <div class="alert alert-danger" role="alert">
                        {{ error }}
                        {% if month %}
                            {{ month }}
                        {% endif %}
                        {% if name %}
                            {{ name }}
                        {% endif %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

</body>